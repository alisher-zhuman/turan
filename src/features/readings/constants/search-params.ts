import type { ReadingsFilters } from "../types";

export const DEFAULT_READINGS_FILTERS: ReadingsFilters = {
  meterId: null,
  customerId: "",
  client: "",
  address: "",
  dateFrom: "",
  dateTo: "",
};
